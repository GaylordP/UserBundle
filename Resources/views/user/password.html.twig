{% extends 'default/template/template.html.twig' %}

{% block title %}
    <i class="{{ include('@User/user/_password_ico.html.twig', with_context = false) }}"></i>
    {{ 'title.user.password'|trans({}, 'user') }}
{% endblock %}

{% block content %}
    {{ include('@User/user/_password_content.html.twig',
        {
            'form': form,
        },
        with_context = false
    ) }}
{% endblock %}