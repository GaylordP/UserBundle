{{ ('@' ~ user.username)|button_link(
    path('member_profile',
        {
            'slug': user.slug,
        }
    ),
    user.color.slug,
    null,
    false,
    {
        'class': 'btn-sm',
    }
) }}