{% extends 'default/template/template.html.twig' %}

{% block title %}
    <i class="{{ include('@User/security/_register_ico.html.twig', with_context = false) }}"></i>
    {{ 'title.register'|trans({}, 'user') }}
{% endblock %}

{% block content %}
    {{ include('@User/security/_register_content.html.twig',
        {
            'form': form,
        },
        with_context = false
    ) }}
{% endblock %}